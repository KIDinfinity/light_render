import { state, effects, reducers } from '../pages/User/_models';

export default {
  namespace: 'login',

  state: {
    ...state,
  },

  effects: {
    ...effects,
  },

  reducers: {
    ...reducers,
  },
};
