import { state, effects, reducers } from 'claim/pages/CaseSplit/_models/splitIncident';

export default {
  namespace: 'caseSplitIncidentController',

  state: {
    ...state,
  },

  effects: {
    ...effects,
  },

  reducers: {
    ...reducers,
  },
};
