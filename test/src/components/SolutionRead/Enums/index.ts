enum EType {
  CIRCLE = 'circle',
  ITEM = 'item',
}
enum EModule {
  SWITCH = 'switch',
  INFORMATION = 'remark',
  ENVOY = 'pending',
  DOC = 'doc',
  Integration = 'integration',
}
enum ESubjectType {
  INFORMATION = 'Information',
  ENVOY = 'Envoy',
  ENVOYMEMO = 'Memo',
  DOC = 'Document',
}
export { EType, EModule, ESubjectType };
