import setPolicyInfo from './setPolicyInfo';
import setTransactionTypes from './setTransactionTypes';
import setSelectedTransactionTypes from './setSelectedTransactionTypes';
import setProcessList from './setProcessList';
import resetState from './resetState';
import saveState from './saveState';

const reducers = {
  setPolicyInfo,
  setTransactionTypes,
  setSelectedTransactionTypes,
  setProcessList,
  resetState,
  saveState,
};

export default reducers;
