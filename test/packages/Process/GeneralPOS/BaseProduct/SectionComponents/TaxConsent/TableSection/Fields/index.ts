import TaxConsentOption, { localFieldConfig as TaxConsentOptionConfig } from './TaxConsentOption';
import PolicyId, { localFieldConfig as PolicyIdConfig } from './PolicyId';

export const localFieldConfigs = [TaxConsentOptionConfig, PolicyIdConfig];

export default {
  TaxConsentOption,
  PolicyId,
};
