import { formatMessageApi } from '@/utils/dictFormatMessage';

export default () => ({
  paymentTrackDetailList: {
    title: formatMessageApi({
      Label_BIZ_Claim: 'paymentTrackDetail',
    }),
  },
});
