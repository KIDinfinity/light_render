export enum SplitClaimTypes {
  NewClaim = 'venus-split_split-with-new-claim-no',
  OriginalClaim = 'venus-split_split-with-original-claim-no',
}

export enum eCategory {
  RequestForm = 'requestForm',
  DeathReport = 'deathReport',
  DiagnosisReport = 'diagnosisReport',
  HospitalizationReport = 'hospitalizationReport',
  TreatmentReport = 'treatmentReport',
  OtherDiagnosisReport = 'otherDiagnosisReport',
  IncidentReport = 'incidentReport',
  AnnuityForm = 'annuityForm',
  // 默认
  DefaultForm = 'defaultForm',
}

export enum SplitDocumentType {
  NewDocument = 'newData',
  OriginalDocument = 'originalData',
}
