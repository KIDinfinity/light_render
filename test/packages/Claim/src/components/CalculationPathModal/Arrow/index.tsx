import React from 'react';
import classNames from './index.less';

class Arrow extends React.PureComponent {
  render() {
    return <span className={classNames.arrow} />;
  }
}

export default Arrow;
