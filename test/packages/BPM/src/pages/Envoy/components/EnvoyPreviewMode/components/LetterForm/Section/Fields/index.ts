import Title, { localFieldConfig as TitleConfig } from './Title';
import DestId, { localFieldConfig as DestIdConfig } from './DestId';
import CcId, { localFieldConfig as CcIdConfig } from './CcId';
import MobileNumber, { localFieldConfig as MobileNumberConfig } from './MobileNumber';

export const localFieldConfigs = [TitleConfig, DestIdConfig, CcIdConfig, MobileNumberConfig];

export default {
  Title,
  DestId,
  CcId,
  MobileNumber,
};
