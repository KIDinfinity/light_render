enum OperationType {
  envoyManualSend = 'envoyManualSend',
}

enum LetterType {
  email = 'email',
  sms = 'sms',
  doc = 'doc',
}

enum SendChannel {
  mc_crpd = 'mc_crpd',
}

export { OperationType, LetterType, SendChannel };
