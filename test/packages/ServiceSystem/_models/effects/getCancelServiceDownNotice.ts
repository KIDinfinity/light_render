export default function* getLoginUsers({ payload }: any, {}: any) {
  const {} = payload;

  // const response = yield cancelServiceDownNotice({ ...params });

  // if (
  //   lodash.isPlainObject(response) &&
  //   response?.success &&
  //   lodash.isPlainObject(response.resultData)
  // ) {
  // }
}
