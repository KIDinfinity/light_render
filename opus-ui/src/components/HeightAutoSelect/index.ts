import { Select } from 'antd';
import styled from 'styled-components';

const HeightAutoSelect = styled(Select)`
  .ant-select-selection {
    height: auto !important;
  }
`;

export default HeightAutoSelect;
