import { state, effects, reducers } from 'claim/pages/CaseSplit/_models/splitPolicy';

export default {
  namespace: 'caseSplitPolicyController',

  state: {
    ...state,
  },

  effects: {
    ...effects,
  },

  reducers: {
    ...reducers,
  },
};
