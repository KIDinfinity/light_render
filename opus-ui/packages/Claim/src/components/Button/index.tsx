import React from 'react';
import AddButton from './AddButton';
import DeleteButton from './DeleteButton';
import ExpandButton from './ExpandButton';
import RefreshButton from './RefreshButton';
import Button from './Button';
import AppealCaseButton from './AppealCaseButton';

const Btn = () => {
  return <div />;
};

Btn.AddButton = AddButton;
Btn.DeleteButton = DeleteButton;
Btn.ExpandButton = ExpandButton;
Btn.RefreshButton = RefreshButton;
Btn.Button = Button;
Btn.AppealCaseButton = AppealCaseButton;

export default Btn;
