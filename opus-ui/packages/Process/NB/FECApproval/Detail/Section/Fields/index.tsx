import AcceptRisk, { localFieldConfig as AcceptRiskConfig } from './AcceptRisk';
import Reason, { localFieldConfig as ReasonConfig } from './Reason';

export const localFieldConfigs = [AcceptRiskConfig, ReasonConfig];

export default {
  AcceptRisk,
  Reason,
};
