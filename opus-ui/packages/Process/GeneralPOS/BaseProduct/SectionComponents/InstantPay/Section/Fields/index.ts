import InstantPayFlag, { localFieldConfig as InstantPayFlagConfig } from './InstantPayFlag';
import PaymentRefNo, { localFieldConfig as PaymentRefNoConfig } from './PaymentRefNo';
export const localFieldConfigs = [PaymentRefNoConfig, InstantPayFlagConfig];

export default {
  PaymentRefNo,
  InstantPayFlag,
};
