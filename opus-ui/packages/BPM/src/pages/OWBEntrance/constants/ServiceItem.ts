export default interface ServiceItem {
  buttonParams: string;
  buttonId: string;
  buttonCode: string;
  buttonName: string;
  activityStatus: string;
  buttonServiceOrder: number;
  afterHook: string;
}
